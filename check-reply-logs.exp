#!/usr/bin/expect -f

set timeout 30
set server "root@45.95.234.173"
set password "o-4zWa6SFWUGo,"

spawn ssh $server

expect {
    "password:" {
        send "$password\r"
    }
}

expect "# "

send "cd /root/TrustStore && pm2 logs trust-store --lines 100 --nostream | grep -i 'ответ\\|reply\\|email\\|отправка' | tail -30\r"
expect "# "

send "exit\r"
expect eof


