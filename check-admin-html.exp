#!/usr/bin/expect -f

set timeout 10
set server "root@45.95.234.173"
set password "o-4zWa6SFWUGo,"

spawn ssh $server

expect {
    "password:" {
        send "$password\r"
    }
}

expect "# "

send "cd /root/TrustStore && grep -n 'data-section=\"emails\"' admin.html\r"
expect "# "

send "grep -n 'id=\"emails\"' admin.html | head -3\r"
expect "# "

send "exit\r"
expect eof


