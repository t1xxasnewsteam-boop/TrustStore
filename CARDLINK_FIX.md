# Исправление обработки Cardlink webhooks

## Что было исправлено

### 1. **Улучшена обработка Result URL webhook**
   - Добавлена поддержка как POST, так и GET запросов
   - Нормализация названий полей (поддержка разных вариантов: `Status`/`status`, `InvId`/`inv_id`, и т.д.)
   - Улучшено логирование для отладки
   - Добавлена обработка данных из query string (для GET запросов)

### 2. **Улучшена обработка Success URL**
   - Добавлена поддержка GET запросов (редирект браузера)
   - Добавлена поддержка POST запросов
   - Нормализация параметров
   - Улучшено логирование

### 3. **Улучшена обработка Fail URL**
   - Добавлена поддержка GET запросов
   - Добавлена поддержка POST запросов
   - Улучшено логирование

### 4. **Улучшена обработка Refund URL**
   - Добавлена нормализация полей
   - Улучшено логирование
   - Исправлена отправка Telegram уведомлений (используется `sendTelegramWithRetry`)

### 5. **Улучшена обработка Chargeback URL**
   - Добавлена нормализация полей
   - Улучшено логирование
   - Исправлена отправка Telegram уведомлений (используется `sendTelegramWithRetry`)

## Основные улучшения

### Нормализация полей
Теперь код поддерживает разные варианты названий полей от Cardlink:
- `Status` или `status`
- `InvId` или `inv_id`
- `OutSum` или `out_sum`
- `CurrencyIn` или `currency_in`
- `Commission` или `commission`
- `SignatureValue` или `signature_value`

### Поддержка разных форматов данных
- POST body (стандартный формат)
- Query string (для GET запросов)
- Комбинация body и query

### Улучшенное логирование
- Подробное логирование всех входящих данных
- Логирование проверки подписи
- Логирование процесса обработки заказа
- Логирование отправки email и Telegram уведомлений
- Логирование ошибок с stack trace

### Обработка подписи
- Проверка подписи с подробным логированием
- Режим отладки (продолжает обработку даже при неверной подписи для отладки)
- В продакшене можно включить строгую проверку подписи

## Настроенные URLs

Все URLs настроены в Cardlink:
- **Success URL:** `https://truststore.ru/api/payment/cardlink/success`
- **Fail URL:** `https://truststore.ru/api/payment/cardlink/fail`
- **Result URL:** `https://truststore.ru/api/payment/cardlink/result`
- **Refund URL:** `https://truststore.ru/api/payment/cardlink/refund`
- **Chargeback URL:** `https://truststore.ru/api/payment/cardlink/chargeback`

## Как проверить работу

1. **Создать тестовый заказ через Cardlink**
2. **Оплатить заказ**
3. **Проверить логи на сервере:**
   ```bash
   pm2 logs trust-store --lines 100
   ```
4. **Проверить, что заказ обновлен в базе данных:**
   ```bash
   sqlite3 analytics.db "SELECT order_id, status, payment_method FROM orders ORDER BY created_at DESC LIMIT 5;"
   ```
5. **Проверить, что email отправлен клиенту**
6. **Проверить, что Telegram уведомление отправлено**

## Что делать, если заказ не обрабатывается

1. **Проверить логи на сервере:**
   ```bash
   pm2 logs trust-store --lines 200 | grep -i cardlink
   ```

2. **Проверить, что webhook приходит:**
   - В логах должны быть записи `📥 CARDLINK WEBHOOK ПОЛУЧЕН`
   - Проверить, какие данные приходят от Cardlink

3. **Проверить подпись:**
   - В логах должна быть строка для подписи
   - Проверить, совпадает ли подпись

4. **Проверить, что заказ существует в базе:**
   ```bash
   sqlite3 analytics.db "SELECT * FROM orders WHERE order_id = 'ORD-XXXXX';"
   ```

5. **Отправить заказ вручную:**
   ```bash
   curl -X POST http://localhost:3000/api/manual-send-last-order \
     -H "Content-Type: application/json" \
     -d '{"orderId": "ORD-XXXXX"}' \
     -s | python3 -m json.tool
   ```

## Статусы заказов

- `awaiting_payment` - заказ создан, ожидает оплаты
- `paid` - заказ оплачен, email отправлен
- `refunded` - заказ возвращен
- `chargeback` - заказ оспорен

## API Token и Shop ID

- **API Token:** `32730|h0MpLJQPQcFdt1ZARaWE7zPys6SrLPoFTZVBW2hM` (используется только часть после `|`)
- **Shop ID:** `9PmRwbY25J`

## Формула подписи

```
signature = MD5(OutSum:InvId:API_TOKEN).toUpperCase()
```

Где:
- `OutSum` - сумма платежа
- `InvId` - ID заказа (order_id)
- `API_TOKEN` - API токен Cardlink (часть после `|`)

## Примечания

- Все webhooks логируют подробную информацию для отладки
- В режиме отладки обработка продолжается даже при неверной подписи
- Email отправляется с повторными попытками (до 3 раз)
- Telegram уведомления отправляются с повторными попытками (до 3 раз)
- Заказы не обрабатываются повторно, если статус уже `paid`

