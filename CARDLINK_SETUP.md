# 💳 Настройка оплаты через Cardlink

## ✅ Что сделано:

1. **Добавлен способ оплаты Cardlink** в checkout.html
2. **Созданы API endpoints**:
   - `/api/payment/cardlink/create` - создание платежа
   - `/api/payment/cardlink/result` - webhook для обработки результата (Result URL)
   - `/api/payment/cardlink/success` - перенаправление после успешной оплаты (Success URL)
   - `/api/payment/cardlink/fail` - перенаправление после неуспешной оплаты (Fail URL)
   - `/api/payment/cardlink/refund` - обработка возврата (Refund URL)
   - `/api/payment/cardlink/chargeback` - обработка чарджбэка (Chargeback URL)

## 🔧 Настройка:

### 1. Получи API токен и Shop ID

1. Зарегистрируйся на [Cardlink](https://cardlink.link)
2. Войди в личный кабинет
3. Перейди в раздел API-интеграций
4. Получи свой уникальный API токен и Shop ID

### 2. Добавь в .env на сервере:

```bash
CARDLINK_API_TOKEN=твой_api_токен
CARDLINK_SHOP_ID=твой_shop_id
```

Или в `server.js` на строках ~5064-5066:
```javascript
const CARDLINK_API_TOKEN = process.env.CARDLINK_API_TOKEN || 'твой_api_токен';
const CARDLINK_SHOP_ID = process.env.CARDLINK_SHOP_ID || 'твой_shop_id';
```

### 3. Настрой URLs в личном кабинете Cardlink

В форме настройки проекта укажи следующие URL:

**Описание магазина:**
```
Оформление зарубежных сервисов
```

**URL магазина:**
```
https://truststore.ru
```

**Success URL (обязательно):**
```
https://truststore.ru/api/payment/cardlink/success
```

**Fail URL (обязательно):**
```
https://truststore.ru/api/payment/cardlink/fail
```

**Result URL (обязательно):**
```
https://truststore.ru/api/payment/cardlink/result
```

**Refund URL (опционально):**
```
https://truststore.ru/api/payment/cardlink/refund
```

**Chargeback URL (опционально):**
```
https://truststore.ru/api/payment/cardlink/chargeback
```

## 📋 Как работает:

1. **Клиент оформляет заказ** → выбирает "Банковская карта"
2. **Создается платеж через Cardlink API** → заказ получает статус `awaiting_payment`
3. **Клиент перенаправляется на страницу оплаты Cardlink**
4. **После оплаты:**
   - Cardlink отправляет webhook на `/api/payment/cardlink/result`
   - Заказ автоматически помечается как `paid`
   - Клиенту отправляется email с товаром
   - Тебе приходит уведомление в Telegram
   - Клиент перенаправляется на `/success`

## 🔐 Проверка подписи:

Все webhook запросы проверяются по формуле:
```
md5(OutSum:InvId:API_TOKEN).toUpperCase()
```

## 📝 Примечания:

- Плательщик оплачивает комиссию (`payer_pays_commission: 1`)
- Валюта платежа: RUB
- При успешной оплате заказ автоматически помечается как оплаченный
- При возврате или чарджбэке приходит уведомление в Telegram

