#!/usr/bin/expect -f

set timeout 10
set server "root@45.95.234.173"
set password "o-4zWa6SFWUGo,"

spawn ssh $server

expect {
    "password:" {
        send "$password\r"
    }
}

expect "# "

send "cd /root/TrustStore && sleep 5 && pm2 logs trust-store --lines 100 --nostream | grep -E 'Синхронизация|обработано|сохранено|INBOX|Spam|Спам|✅|❌' | tail -15\r"
expect "# "

send "exit\r"
expect eof


