#!/usr/bin/expect -f

set timeout 15
set server "root@45.95.234.173"
set password "o-4zWa6SFWUGo,"

spawn ssh $server

expect {
    "password:" {
        send "$password\r"
    }
}

expect "# "

send "cd /root/TrustStore && git pull origin main && pm2 restart trust-store --update-env\r"
expect "# "

send "sleep 25 && pm2 logs trust-store --lines 100 --nostream | grep -E 'Синхронизация|INBOX|Spam|Спам|обработано|сохранено|📧|📬|🚨|✅' | tail -40\r"
expect "# "

send "exit\r"
expect eof


