#!/usr/bin/expect -f

set timeout 15
set server "root@45.95.234.173"
set password "o-4zWa6SFWUGo,"

spawn ssh $server

expect {
    "password:" {
        send "$password\r"
    }
}

expect "# "

send "cd /root/TrustStore && git pull origin main && pm2 restart trust-store --update-env\r"
expect "# "

send "sleep 8 && pm2 status\r"
expect "# "

send "pm2 logs trust-store --err --lines 10 --nostream | tail -5\r"
expect "# "

send "exit\r"
expect eof


